<!DOCTYPE html>
<head>
</head>
<body>

<% render "shared/footer.html.erb" %>



<% @workers.each do |worker| %>
<p><%= worker.name %> 
<%= worker.lastname %> 
<%= worker.occupation %> 
<%= worker.phone %> 
<%= worker.descript %> 
<%= link_to 'Destroy', worker, :method => :delete, :confirm => 'Are you sure?' %>

<% end %>

</p>


</body>
